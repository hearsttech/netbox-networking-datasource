Circuit ID, Type, Status, Provider, Provider Account, Tenant, Site Name, Termination Site, Termination Device, Termination Port, Speed Mbps, Termination Provider, IP, Install Date, Termination Date
{% for circuit in queryset -%}
"{{ circuit.cid }}","{{ circuit.type }}","{{ circuit.status }}","{{ circuit.provider.name }}","{{ circuit.provider_account.name }}","{{ circuit.tenant.name }}","{{ circuit.termination_a._site.name if circuit.termination_a and circuit.termination_a._site else '' }}","{{ circuit.termination_a.termination.name if circuit.termination_a and circuit.termination_a.termination else '' }}","{{ circuit.termination_a.cf.terminatingDevice if circuit.termination_a else '' }}","{{ circuit.termination_a.cf.Interface if circuit.termination_a else '' }}","{{ circuit.termination_a.port_speed/1000 if circuit.termination_a and circuit.termination_a.port_speed else '' }}","{{ circuit.termination_z.termination.name if circuit.termination_z and circuit.termination_z.termination else '' }}", {{ circuit.termination_a.cf.ip if circuit.termination_a else '' }},"{{ circuit.install_date }}","{{ circuit.termination_date }}"
{% endfor -%}